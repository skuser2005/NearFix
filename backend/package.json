{
  "name": "nearfix-backend",
  "version": "1.0.0",
  "private": true,
  "description": "NearFix backend API and static frontend server",
  "scripts": {"start":"node server.js","dev":"node --watch server.js"},
  "engines": {"node": ">=18"}
}
